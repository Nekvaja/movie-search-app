export interface Theme {
    colors: {
        primary: string,
        secondary: string,
        accent: string,
        background: string,
        textPrimary: string,
        textSecondary: string,
        placeholderText: string,
    }
}